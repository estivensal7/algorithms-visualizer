import React, { useState } from "react";
import {
	Collapse,
	Navbar,
	NavbarToggler,
	NavbarBrand,
	Nav,
	NavItem,
	NavLink,
} from "reactstrap";

const Landing = (props) => {
	return (
		<div>
			<h1>Landing Page</h1>
		</div>
	);
};

export default Landing;
