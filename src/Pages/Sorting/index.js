import React, { useState } from "react";
import {
	Collapse,
	Navbar,
	NavbarToggler,
	NavbarBrand,
	Nav,
	NavItem,
	NavLink,
} from "reactstrap";

const Sorting = (props) => {
	return (
		<div>
			<h1>Sorting Page</h1>
		</div>
	);
};

export default Sorting;
